Code annotations for Lane2025QJE

Primary annotation files in this folder preserve the original source-code
extensions. Stata scripts remain .do files, and R scripts remain .R files.

These files are annotated copies. They do not replace or modify the original
replication package files. Each annotated file starts with a comment header
that records source path, role, and audit linkage, followed by the copied
original code.

Earlier Markdown prose notes were generated during the first audit pass and
are now archived under:

code_annotations/markdown_notes_archive/

Primary annotated source-code files: 34
